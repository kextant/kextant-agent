apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: kextant-reader-binding
  labels:
    app.kubernetes.io/name: kextant
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: kextant-reader
subjects:
  - kind: ServiceAccount
    name: kextant-agent
    namespace: kextant
